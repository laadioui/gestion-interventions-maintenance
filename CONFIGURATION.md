# Configuration

Le fichier réel `src/main/resources/config.properties` est ignoré par Git pour éviter les secrets.

Clés disponibles :

```properties
db.url=jdbc:mysql://localhost:3306/gestion_maintenance
db.user=root
db.password=
app.name=MaintenX
app.version=1.0.0
```

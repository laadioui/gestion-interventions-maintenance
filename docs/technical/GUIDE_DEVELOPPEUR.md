# Guide développeur

Compiler :

```bash
mvn clean test
```

Lancer :

```bash
java -jar target/maintenx.jar
```

Ajouter une fonctionnalité consiste à créer ou modifier la vue, le contrôleur, le service, puis le DAO si une persistance MySQL est nécessaire.
